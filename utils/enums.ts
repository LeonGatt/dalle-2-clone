export enum CanvasElement {
  Frame = "frame",
  Image = "image",
  OriginalImage = "originalImage",
  OriginalImageEl = "originalImageEl",
  Group = "group",
}

export enum AppStore {
  konva = "konva",
  paint = "paint",
  image = "image",
}

export enum ImageSizes {
  small = "256x256",
  medium = "512x512",
  large = "1024x1024",
}

export enum NumberOfImages {
  small = 1,
  medium = 3,
  large = 5,
}

export enum LineJoinType {
  round = "round",
  butt = "butt",
  square = "square",
}

export enum CanvasRenderingContext {
  sourceOver = "source-over",
  sourceIn = "source-in",
  sourceOut = "source-out",
  sourceAtop = "source-atop",
  destinationOver = "destination-over",
  destinationIn = "destination-in",
  destinationOut = "destination-out",
  destinationAtop = "destination-atop",
  lighter = "lighter",
  copy = "copy",
  xor = "xor",
  multiply = "multiply",
  screen = "screen",
  overlay = "overlay",
  darken = "darken",
  lighten = "lighten",
  colorDodge = "color-dodge",
  colorBurn = "color-burn",
  hardLight = "hard-light",
  softLight = "soft-light",
  difference = "difference",
  exclusion = "exclusion",
  hue = "hue",
  saturation = "saturation",
  color = "color",
  luminosity = "luminosity",
}
